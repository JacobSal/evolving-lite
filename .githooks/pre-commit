#!/usr/bin/env bash
# Pre-commit leak-scan gate. Enable once per clone:
#   git config core.hooksPath .githooks && chmod +x .githooks/pre-commit
# (git silently skips non-executable hooks - the chmod matters)
exec python3 "$(git rev-parse --show-toplevel)/scripts/dev/leak-scan.py"
